# ==============================================================================
# Orbital Shield — Environment Configuration Template
# Copy this file to .env or .env.local if custom environment overrides are needed
# ==============================================================================

# Backend API Configuration
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Frontend Configuration (Optional override if running API on a non-default host/port)
VITE_API_URL=http://localhost:8000
